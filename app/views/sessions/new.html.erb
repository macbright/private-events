<div class="container">
  <h1>Log In</h1>
  ​
  <div class="row">
    <div class="col-md-6 col-md-offset-3">
      <%= form_for(:session, url: login_path) do |f| %>
        <div class="form-group">
          <%= f.label :email %>
          <%= f.email_field :email, class: 'form-control session' %>
  ​      </div>

        <div class="form-group">
          <%= f.submit "Log in", class: "btn btn-primary" %>
        </div>
      <% end %>

      <p>New user? <%= link_to "Sign up now", signup_path %></p> 
      <p>Note: You can sign in "john@example.com" to test this Demo App.</p>
  ​
    </div>
  </div>

</div>