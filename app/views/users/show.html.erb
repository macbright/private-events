
<% if logged_in? %>

  <div class="row">
    <div class="col-md-3 ">
      <%= render 'events/event_form' %>
  </div>
    
  <div class="col-md-6">
      <h2> Upcoming Events </h2>
      <%= render @upcoming_events %><br>

      <h2> Past Events </h2>
      <%= render @previous_events %>
     
  </div>

   <div class="col-md-3">
    <h5> Attending Events</h5>
      <%= render @list_events %>
  </div>
</div>

<% else %>
  <h2> Please sign in. <h2>
  <%= link_to "Sign in", login_path %>
<% end %>





